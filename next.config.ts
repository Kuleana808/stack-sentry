/**
 * Present at the repository root so Vercel detects the Next.js framework when
 * the project Root Directory is `.`. The app still lives in apps/web;
 * `npm run build` runs that workspace. Do not set `outputDirectory` in
 * vercel.json — the Next.js builder uses `.next`.
 */
export { default } from './apps/web/next.config'
